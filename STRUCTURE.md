# 5-Folder Structure

This package is organized into exactly five folders for easier GitHub publishing and deployment:

1. `client`
2. `server`
3. `config`
4. `docs`
5. `logs`

The frontend is served directly by the Express server, so the project can be deployed as a single Node.js app without a separate client build step.
